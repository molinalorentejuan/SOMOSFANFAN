{
  "name": "fanfan-fullstack",
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "build": "npm install --prefix web && npm run build --prefix web && npm install --prefix api && npm run build --prefix api",
    "start": "NODE_ENV=production node server.js"
  },
  "private": true
}
